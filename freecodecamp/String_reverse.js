// 1 Reverse a string
// Note: there is no reverse for a string but there is reverse for an array
//to reverse a string we use arr.reverse()
const we1 = "happy";

function reverseString(str) {
    // We convert the str to array first
    let we1 = "rty"
    let arrRev = str.split("")
    console.log(we1)
    return arrRev.reverse().toString();
}

console.log(reverseString("Victoria"))

//.split()
//.reverse()
//.toString()
